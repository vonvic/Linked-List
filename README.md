# LinkedList1

To build, use
```
make build
```

To run, use
```
make run
```

To test, use
```
make test
```
**As of right now, there are no test files. Running `make test` will result in an error.*
